package cucumberTest;

import java.util.concurrent.TimeUnit;

import org.junit.runner.RunWith;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.support.ui.FluentWait;
import org.openqa.selenium.support.ui.Wait;

/*
 * import io.cucumber.junit.Cucumber; import io.cucumber.junit.CucumberOptions;
 * 
 * @RunWith(Cucumber.class)
 * 
 * @CucumberOptions(features = "Feature", glue = "StepDefinition", // dryRun =
 * true, monochrome = true, tags = {}, strict = true, plugin = { "pretty",
 * "html:target/cucumber" })
 * 
 * public class TestQA {
 * 
 * }
 */
/*public class TestQA {

	public static void main(String[] args) {
		
		 * DesiredCapabilities capabilities = DesiredCapabilities.firefox();
		 * capabilities.setCapability("marionette", true);
		 
		
		 * System.setProperty("webdriver.gecko.driver",
		 * "D:\\Softwares\\geckodriver.exe");
		 * 
		 * WebDriver driver = new FirefoxDriver(); driver.get("http://www.toolsqa.com");
		 * FluentWait f = new FluentWait(driver); f.pollingEvery(null);
		 * f.ignoring(NullPointerException.class);
		 * 
		 * driver.quit();
		 
		System.out.println("*************************************************");
	}

 } */
